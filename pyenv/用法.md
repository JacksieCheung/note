## pyenv 指令记录

```shell
pyenv versions # 列出目录所有安装版本

pyenv version # 现实目前版本

pyenv global <python_version> # 切换版本

pyenv install <python_version> # 安装指定版本

pyenv uninstall <python_version> # 移除指定版本
```
